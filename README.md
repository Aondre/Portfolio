# Professional Portfolio:

## Project Notes:

- npx create-next-app@latest . --typescript. This command allowed me to install the next.js framework into my local repo instead of creating a project.
- Originally, the repo's name was Portfolio but next.js has a lowercase only naming convention.
- Removed all files under public, along with the folder itself.
- Had an issue with the next.js logo still remaining on the page. After deleting & clearing browser cache, it remained. Learned that it was cached in the .next folder. After removing that, the logo vanished.
